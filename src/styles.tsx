import { StyleSheet } from "react-native";

export const SharedStyles = StyleSheet.create({
  pageHeaderView: {
    alignItems: "center",
    justifyContent: "center",
  },
  pageHeaderText: {
    fontSize: 30,
    fontWeight: "bold",
    paddingVertical: 20,
  },
});
