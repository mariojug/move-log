import { StyleSheet, Text, View } from "react-native";
import React from "react";

// TODO: create logic to pull and display all workouts
// TODO: add logic to update Routine - notes, days of week
// TODO: add logic to create sections -
//      - name section
//      - add workouts with params
// TODO: add logic to update sections - name, workouts in section
// TODO: add logic to reorder sections
// TODO: add logic to reorder workouts (within/betweens sections)
const Routine = () => {
  return (
    <View>
      <Text>Routine</Text>
    </View>
  );
};

export default Routine;

const styles = StyleSheet.create({});
